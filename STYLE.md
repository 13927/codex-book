# 《Codex 技术解析》写作规范

本规范是全书唯一标准。任何章节的新写与改写必须逐条满足；校验脚本与人工走查以此为准。

## 0. 读者设定

零基础读者：会写代码，但不懂 Rust、没接触过 coding agent 内部概念。
总原则：**任意一章按顺序读，不会遇到"出现了但没解释过"的术语。**

## 1. 章节模板（七节，标题固定、顺序固定）

1. `## 本章导读` —— 从一个具体问题或场景切入（例："对话太长装不下，Codex 怎么办？"），
   零黑话；然后列学习目标（大白话）与前置章节。
2. `## 概念与架构` —— 只用类比 + mermaid 图 + 大白话。**禁止**出现代码标识符、
   文件路径、行号。允许极个别无法回避的专有名词，但必须当场用大白话解释。
3. `## 出场角色` —— 一张表：`中文名 | 英文名 | 职责（一句话） | 所在文件`。
   覆盖后文「源码深挖」将出现的所有重要类型/模块/函数。
4. `## 源码深挖` —— 每个小节（`###`）开头先写一段导览散文：这段讲什么、谁会出场、
   读完能知道什么；然后才给细节。表格列名一律中文。
5. `## 技术难点与设计取舍` —— 每个难点一段：问题是什么、为什么难、Codex 怎么选、
   代价是什么。
6. `## 对照通用 agent 范式` —— 把本章设计映射回通用概念（ReAct、上下文工程等）。
7. `## 小结与下一章预告` —— 要点清单 + 下一章悬念。

## 2. 术语规则（首现注）

- 英文标识符（类型名、函数名、模块名、crate 名、协议名、专有名词）在**每章首次出现**时：
  `中文名（English）——一句话职责`（正文行文）或表格行（出场角色表）。
  同章后文可单用中文名，也可中英混用，但中文名必须与首现注一致。
- 中文名全书唯一：同一个 English 术语在所有章节必须用同一个中文名（以术语表为准）。
- 代码块、行号引用、`路径` 中的标识符保持英文原文，不翻译。
- 「概念与架构」节不适用首现注——该节根本不允许出现代码标识符（见 §1）。

## 3. 图表规则

- mermaid 节点标签中文优先；英文标识符只允许放在括号内，例：`会话核心（Session）`。
- 图的箭头标注用中文短语，不用函数名。
- 每章至少 1 张 mermaid 图，图前后各有一句话说明"这张图看什么"。

## 4. 引用规则

- 行号引用格式：`仓库相对路径#L起-L止`，基线 commit `9e868bd9dc`。
- 引用数量只增不减：改写后每章行号引用数不得少于旧稿。
- 引用必须真实：写入前用 `git show 9e868bd9dc:<path>` 核对符号确实在该行附近。

## 5. 行文规则

- 一句话只说一件事；避免超过三行的长句。
- 每节信息自包含：不依赖读者记得前一章的术语（跨章引用用"（见第 N 章）"提示）。
- 禁用：黑话轰炸（一句话三个以上未解释缩写）、无类比的定义堆砌、"显然/众所周知"。
